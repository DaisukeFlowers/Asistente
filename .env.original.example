# Original / Production-like Environment Variables Template
# Keep this as a reference to the values used in the first deployed (Render) configuration.
# Copy needed keys into your real .env or platform environment settings. Never commit secrets.

# Flask secret key (set via platform env vars in production)
SECRET_KEY=

# Google OAuth (production project)
CLIENT_ID=
CLIENT_SECRET=
REDIRECT_URI=https://your-production-backend.example.com/api/auth/google/callback

# Google Calendar scope (restrict if possible, justify in verification)
GOOGLE_SCOPE=https://www.googleapis.com/auth/calendar

# n8n production webhook URL
N8N_WEBHOOK_URL=

# Alternate accepted names (leave blank unless you specifically need them)
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# OAUTH_REDIRECT_URI=

# Backend port (usually injected by platform)
PORT=3000
