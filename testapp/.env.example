# Copy this file to .env and fill in your Google OAuth Web Client ID.
# NEVER commit your real production values.
# For Vite, variables must start with VITE_

VITE_GOOGLE_CLIENT_ID=YOUR_GOOGLE_OAUTH_WEB_CLIENT_ID

# Do not add secrets here. Only VITE_* keys are exposed to the browser.
# Remove legacy REACT_APP_* usage; backend uses CLIENT_ID/CLIENT_SECRET privately.
